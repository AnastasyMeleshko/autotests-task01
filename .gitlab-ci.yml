stages:
  - lint
  - test

lint_job:
  stage: lint
  image: node:20
  script:
    - npm ci
    - npm run lint

test_job:
  stage: test
  image: node:20
  script:
    - npm ci
    - npm run coverage
  artifacts:
    paths:
      - coverage
